# Scenario : Users visit k8s plugin status report page

scenario do
  count 20
  rampup 100
  name 'k8s plugin status report'
  loop do
    url '/admin/status_reports/cd.go.contrib.elasticagent.kubernetes/cluster/k8-cluster-profile'
  end
end
